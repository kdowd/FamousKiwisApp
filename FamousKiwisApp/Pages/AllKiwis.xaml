<Page x:Class="FamousKiwisApp.Pages.AllKiwis"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FamousKiwisApp.Pages"
      xmlns:components="clr-namespace:FamousKiwisApp.Components"
      xmlns:vm="clr-namespace:FamousKiwisApp.ViewModels"
      mc:Ignorable="d" 
      
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AllKiwis">
    <Page.DataContext>
        <vm:AllKiwisCollection x:Name="KiwiCollectionAlias" />
    </Page.DataContext>

    <Grid>
        <TextBlock Text="List of Kiwis" FontSize="60" Foreground="White"  />

        <ItemsControl Grid.Row="1" Margin="0,20,0,0" ItemsSource="{Binding AllKiwisEnumerable}" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <components:KiwiCard Width="200" Foreground="White" Background="OrangeRed" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel>
                    </WrapPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

    </Grid>
</Page>
